#### Results (`r params$year_start`-`r params$year_end`)

`r params$msg`

Additional information:

- K-statistic: `r round(params$k_statistic, 2)`
- K-statistic significance threshold: `r round(params$k_alpha, 2)`

<!-- Render the figure. -->

```{r, echo = FALSE, out.width = "720px", fig.align = "center"}
img_name <- glue("img/pettitt-test-{params$year_start}-{params$year_end}.png")
include_graphics(file.path(params$output_dir, img_name))
```
