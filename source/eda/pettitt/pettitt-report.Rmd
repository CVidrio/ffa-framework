### Pettitt Test `r if(params$show_years) glue("({params$start}-{params$end})")`

For more information, see [here](https://rileywheadon.github.io/ffa-framework/eda/pettitt/). 

`r params$msg`

Additional information:

- K-statistic: `r round(params$k_statistic, 2)`
- K-statistic significance threshold: `r round(params$k_alpha, 2)`

<!-- Render the figure. -->

```{r, echo = FALSE, out.width = "80%", fig.align = "center", dpi = 150}
img_name <- glue("img/pettitt-test-{params$start}-{params$end}.png")
include_graphics(file.path(params$output_dir, img_name))
```
