### BB-MK Test `r if(params$show_years) glue("({params$start}-{params$end})")`

For more information, see [here](https://rileywheadon.github.io/ffa-framework/eda/#bb-mk-test). 

`r params$msg`

Additional information:

- Bootstrap samples: `r length(params$s_bootstrap)`
- S-statistic: `r round(params$s_statistic, 2)`
- Confidence bounds: (`r round(params$bounds, 2)`)

<!-- Render the figure. -->

```{r, echo = FALSE, out.width = "80%", fig.align = "center", dpi = 150}
img_name <- glue("img/bbmk-test-{params$start}-{params$end}.png")
include_graphics(file.path(params$output_dir, img_name))
```
