---
title: "Spearman Test Report"
output: html_document:
    css: ../styles.css
  html_notebook:
    css: ../styles.css
params: 
  output_dir: "[output_dir]"
  include_details: TRUE
  include_code: TRUE
  alpha: "[alpha]"
  rho: "[rho]"
  sig: "[sig]"
  least_lag: "[least_lag]"
  msg: "[msg]"
---

### Spearman Test

The **Spearman Test** is used to identify autocorrelation in a time series $y_{t}$.
A *significant lag* is a number $i$ such that the correlation between $y_{t}$ and $y_{t-i}$ is statistically significant.
The *least insignificant lag* is the largest $i$ such that all $j < i$ are significant lags.

- Null hypothesis: The least insignificant lag is $0$.
- Alternative hypothesis: The least insignificant lag is greater than $0$.

<!-- Render additional details about the test. -->
```{r, child = if(include_details) 'spearman-details.Rmd' }
```

<!-- Results -->

#### Results

`r params$msg`

<!-- Render the figure. -->

#### Figure

```{r, echo = FALSE, out.width = "720px", fig.align = "center"}
include_graphics(file.path(params$output_dir, "img/spearman-test.png"))
```

<!-- Include code used to compute the results. -->

```{r, echo = FALSE, include = include_code } 
asis_output("#### Testing Code") 
```

```{r, file = "spearman-test.R", include = include_code, eval = FALSE } 

```

```{r, echo = FALSE, include = include_code } 
asis_output("#### Plotting Code") 
```

```{r, file = "spearman-plot.R", include = include_code, eval = FALSE } 

```

