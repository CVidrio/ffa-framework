### Sen's Trend Estimator `r if(params$show_years) glue("({params$start}-{params$end})")`

**Dataset**: AMS `r tools::toTitleCase(params$data_type)`.

For more information, see [here](https://rileywheadon.github.io/ffa-framework/eda/sens/). 

`r params$msg`

<!-- Render the figure. -->

```{r, echo = FALSE, out.width = "80%", fig.align = "center", dpi = 150}
img_name <- glue("img/sens-{params$data_type}-estimator-{params$start}-{params$end}.png")
include_graphics(file.path(params$output_dir, img_name))
```
